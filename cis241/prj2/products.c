#include <stdio>
#include <string.h>
struct product{
	string name;
	int quantity;
	int price;
}
int choice(int argc, char* argv[]){
if(1 = argc){
product *head = (product *)malloc(sizeof(product);
d -> d = ‘n’; 
head -> next = NULL;
}
if(2 = argc){
string name = argv[];

}
}
